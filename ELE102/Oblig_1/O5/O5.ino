void setup() 
{
  Serial.begin(9600);
  Serial.println("Setup");
}

void loop()
{
  {
  int year=2019;
  int month=1;
  int day=1;
  int hour=0;
  int minute=0;
  int second=0;

  // January
    while (day < 32)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
  // Febuary
    while (day < 28)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    

    // March
    while (day < 31)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // April
    while (day < 30)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // May
    while (day < 31)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // June
    while (day < 30)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // July
    while (day < 31)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // August
    while (day < 31)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // September
    while (day < 30)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // October
    while (day < 31)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // November
    while (day < 30)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
    
    // December
    while (day < 31)
      {
      while (hour < 24)
      {
      while (minute < 60)
        {
          while (second < 60)
          {
          Serial.print(year);
          Serial.print("/");
          Serial.print(month);
          Serial.print("/");
          Serial.print(day);
          Serial.print(" ");
          Serial.print(hour);
          Serial.print(":");
          Serial.print(minute);
          Serial.print(":");
          Serial.println(second);
          delay(1000);
          second = second + 1;
          }
        minute = minute + 1;
        second = 0;    
        }
      hour = hour + 1;
      minute = 0;
      }
    day = day + 1;
    hour = 0;
    }
    month = month + 1;
    day = 1;
  }
}
